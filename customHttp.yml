customHeaders:
  - pattern: '</^[^.]+$|\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|ttf|map|json)$)([^.]+$)/>'
    headers:
      - key: 'content-type'
        value: 'application/octet-stream'
      - key: 'Accept-Ranges'
        value: 'bytes'